class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        v = {}
        for i in range(len(nums)):
            v[nums[i]] = i
        
        for i in range(len(nums)):
            tmp = target - nums[i]
            if tmp in v and v[tmp] != i:
                return [i,v[tmp]]
